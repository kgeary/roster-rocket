import React from "react";
import { ExportToCsv } from 'export-to-csv';



csvExporter.generateCsv(data){

const csvExporter = new ExportToCsv(data);
const data = <CardCourse />;
console.log(data);
};


// Messing around with some of the example code from https://www.npmjs.com/package/export-to-csv}
// npm install --save export-to-csv
// Patrick